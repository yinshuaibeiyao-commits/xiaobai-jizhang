# 小白记账 App — 产品文档

## 1. 项目概述

- **名称**：小白记账
- **定位**：面向个人/家庭的本地记账应用，简单易用
- **运行平台**：Windows 10/11 与 macOS（桌面端）
- **技术栈**：Electron + React + TypeScript + 本地 JSON 文件存储
- **数据存储**：本地 JSON 文件（`userData/xiaobai-jizhang.json`），数据仅保存在本机，无需联网

## 2. 核心功能（首版 · 精简版）

1. **记一笔**：记录每笔花销 —— 金额（人民币）+ 两级分类 + 日期 + 备注（可选）
2. **流水列表**：按时间倒序查看所有花销记录，可按分类 / 日期筛选
3. **编辑 / 删除**：修改或删除已有记录
4. **分类选择**：从预设的两级分类中选取

> 统计图表（按月 / 分类汇总）属于后续版本，不在首版范围。

## 3. 两级分类设计（9 大类 + 二级小类）

| 一级大类 | 二级小类 |
|---|---|
| 餐饮 | 早餐 / 午餐 / 晚餐 / 外卖 / 零食饮料 / 聚餐 |
| 交通 | 公交地铁 / 打车 / 加油充电 / 停车 / 火车飞机 / 共享单车 |
| 购物 | 服饰鞋包 / 数码家电 / 日用百货 / 美妆护肤 |
| 居住 | 房租 / 水电燃气 / 物业费 / 家居维修 / 话费宽带 |
| 娱乐 | 电影演出 / 游戏 / 旅行 / 运动健身 / 会员订阅 |
| 医疗 | 药品 / 门诊 / 体检 |
| 教育 | 书籍 / 课程培训 / 学费 |
| 人情 | 红包 / 礼物 / 请客 |
| 其他 | 其他 |

## 4. 数据模型（花销记录 ExpenseRecord）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | string (UUID) | 主键 |
| amount | number | 金额，单位元，保留 2 位小数，> 0 |
| categoryL1 | string | 一级大类 |
| categoryL2 | string | 二级小类 |
| date | string | YYYY-MM-DD |
| note | string | 备注（可选） |
| createdAt | string | 创建时间戳 |
| updatedAt | string | 更新时间戳 |

## 5. 技术栈

- 桌面壳：Electron
- 前端：React 18 + TypeScript + Vite
- 持久化：本地 JSON 文件（Node `fs` 读写，无原生依赖，免编译）
- 打包：electron-builder（Windows 出 exe / macOS 出 dmg）

## 6. 开发协作规则（重要，全项目遵守）

> 在整个项目开发过程中，凡遇到「技术选型 / 实现方案 / 架构设计 / 功能取舍」等需要做决定的场景，Claude 必须：
>
> 1. 列出多个可行方案；
> 2. 向用户解释各方案的优劣势、适用场景与推荐；
> 3. 由用户最终决定，Claude 不得擅自替用户拍板。
>
> 仅当用户明确授权（如「你定」「你决定就行」）时，Claude 才可直接决定。此规则与「技术选型由用户决定」一并长期遵守。

## 7. 里程碑（Roadmap）

- **M1**：搭建项目骨架 + 产品文档（本步）
- **M2**：实现记账 + 流水列表 + 编辑/删除 + 分类选择
- **M3**：打包发布 Windows + macOS 安装包
- **M4**（后续）：统计图表、预算、数据导出等
